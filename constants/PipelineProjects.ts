import { ResourcePrefix } from './constants';

export const PipelineProjects = {
  BuildConfigPipelineProject: `${ResourcePrefix}-build-config-pipeline-project`,
  AppDeployPipelineProject: `${ResourcePrefix}-app-deploy-pipeline-project`
};
